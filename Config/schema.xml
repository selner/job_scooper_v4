<!--
    Awesome, your propel set up is nearly done! You just have to describe how you want your database to look like.

    You can let propel set up your sqlite database by running `vendor/bin/propel database:create && vendor/bin/propel database:insert-sql`.
    This will create your database including all the tables.
-->

<!--
    The root tag of the XML schema is the <database> tag.

    The `name` attribute defines the name of the connection that Propel uses for the tables in this schema. It is not
    necessarily the name of the actual database. In fact, Propel uses some configuration properties to link a connection
    name with real connection settings (like database name, user and password).

    The `defaultIdMethod` attribute indicates that the tables in this schema use the database's "native"
    auto-increment/sequence features to handle id columns that are set to auto-increment.

   [TIP]: You can define several schemas for a single project. Just make sure that each of the schema
          filenames end with schema.xml.
-->
<database name="default" defaultIdMethod="native"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:noNamespaceSchemaLocation="http://xsd.propelorm.org/1.6/database.xsd"
          namespace="JobScooper\DataAccess"
          defaultPhpNamingMethod="clean"
>
    <!-- Within the <database> tag, Propel expects one <table> tag for each table -->


    <!--
        Each table element should have a `name` attribute. It will be used for naming the sql table.

        The `phpName` is the name that Propel will use for the generated PHP class. By default, Propel uses a
        CamelCase version of the table name as its phpName - that means that you could omit the `phpName` attribute
        on our `book` table.
    -->
    <!--
        Each table element should have a `name` attribute. It will be used for naming the sql table.

        The `phpName` is the name that Propel will use for the generated PHP class. By default, Propel uses a
        CamelCase version of the table name as its phpName - that means that you could omit the `phpName` attribute
        on our `book` table.
    -->
    <table name="jobposting" phpName="JobPosting">
        <!--
            Each column has a `name` (the one used by the database), and an optional `phpName` attribute. Once again,
            the Propel default behavior is to use a CamelCase version of the name as `phpName` when not specified.

            Each column also requires a `type`. The XML schema is database agnostic, so the column types and attributes
            are probably not exactly the same as the one you use in your own database. But Propel knows how to map the
            schema types with SQL types for many database vendors. Existing Propel column types are:
            `boolean`, `tinyint`, `smallint`, `integer`, `bigint`, `double`, `float`, `real`, `decimal`, `char`,
            `varchar`, `longvarchar`, `date`, `time`, `timestamp`, `blob`, `clob`, `object`, and `array`.

            Some column types use a size (like `varchar` and `int`), some have unlimited size (`longvarchar`, `clob`,
            `blob`).

            Check the (schema reference)[http://propelorm.org/reference/schema.html] for more details
            on each column type.

            As for the other column attributes, `required`, `primaryKey`, and `autoIncrement`, they mean exactly
            what their names imply.
        -->
        <column name="jobposting_id" phpName="JobPostingId" type="integer" required="true" primaryKey="true"
                autoIncrement="true"/>

        <!--
            Alternate unique key for every job post is jobsite_key + jobsite post id
        -->
        <column name="jobsite_key" phpName="JobSiteKey" type="varchar" size="100" required="true"/>
        <foreign-key foreignTable="job_site" onUpdate="none" onDelete="none">
            <reference local="jobsite_key" foreign="jobsite_key"/>
        </foreign-key>


        <column name="jobsite_post_id" phpName="JobSitePostId" type="varchar" size="255" required="true"/>
        <unique name="index_jobsite_and_post_id/">
            <unique-column name="jobsite_key"/>
            <unique-column name="jobsite_post_id"/>
        </unique>

        <!--
            Job posting facts
        -->
        <column name="title" phpName="Title" type="varchar" size="255" required="true"/>
        <column name="title_tokens" phpName="TitleTokens" type="varchar" size="255" required="false"/>
        <column name="url" phpName="Url" type="varchar" size="1024" required="true"/>
        <column name="employment_type" phpName="EmploymentType" type="varchar" size="100" required="false"/>
        <column name="pay_range" phpName="PayRange" type="varchar" size="100" required="false"/>
        <column name="company" phpName="Company" type="varchar" size="100" required="false"/>
        <column name="location" phpName="Location" type="varchar" size="255" required="false"/>
        <column name="department" phpName="Department" type="varchar" size="255" required="false"/>
        <column name="category" phpName="Category" type="varchar" size="100" required="false"/>

        <!--
            Date & times for various events related to this particular job posting
        -->
        <column name="last_updated_at" phpName="UpdatedAt" type="timestamp" required="true"/>
        <column name="job_posted_date" phpName="PostedAt" type="timestamp" required="false"/>
        <column name="first_seen_at" phpName="FirstSeenAt" type="timestamp" required="true"/>
        <column name="post_removed_at" phpName="RemovedAt" type="timestamp" required="false"/>
        <behavior name="timestampable">
            <parameter name="create_column" value="first_seen_at"/>
            <parameter name="update_column" value="last_updated_at"/>
        </behavior>

        <!--
            The following columns are automatically set via code in the JobPosting class
        -->
        <column name="key_site_and_post_id" phpName="KeySiteAndPostID" type="varchar" size="255" required="true"
                primaryString="true"/>
        <column name="key_company_and_title" phpName="KeyCompanyAndTitle" type="varchar" size="255" required="true"/>
        <column name="title_linked" phpName="JobTitleLinked" type="varchar" size="1024" required="true"/>

        <!--
            GeoLocation Data for the Job Posting
        -->
        <column name="location_display_value" phpName="LocationDisplayValue" type="varchar" size="255"
                required="false"/>
        <column name="geolocation_id" phpName="GeoLocationId" type="integer" required="false"/>
        <foreign-key foreignTable="geolocation" onUpdate="none" onDelete="none">
            <reference local="geolocation_id" foreign="geolocation_id"/>
        </foreign-key>

        <column name="duplicates_posting_id" phpName="DuplicatesJobPostingId" type="integer" required="false"/>
        <foreign-key foreignTable="jobposting" onUpdate="none" onDelete="none">
            <reference local="duplicates_posting_id" foreign="jobposting_id"/>
        </foreign-key>

    </table>

    <table name="geolocation" phpName="GeoLocation" reloadOnUpdate="true" reloadOnInsert="true">
        <column name="geolocation_id" phpName="GeoLocationId" type="integer" required="true" primaryKey="true"
                autoIncrement="true"/>
        <column name="display_name" phpName="DisplayName" type="varchar" size="100" required="true" primaryString="true"/>
        <column name="geolocation_key" phpName="GeoLocationKey" type="varchar" size="100" required="true"/>
        <!--<behavior name="sluggable">-->
            <!--<parameter name="slug_column" value="geolocation_key"/>-->
            <!--<parameter name="slug_pattern" value="{CountryCode}_{RegionCode}_{Place}"/>-->
            <!--<parameter name="replace_pattern" value="/[^\w\/]+/u"/>-->
            <!--<parameter name="replacement" value=""/>-->
            <!--<parameter name="separator" value="-"/>-->
            <!--<parameter name="permanent" value="false"/>-->
        <!--</behavior>-->

        <column name="place" phpName="Place" type="varchar" size="100"/>
        <column name="county" phpName="County" type="varchar" size="100"/>
        <column name="region" phpName="Region" type="varchar" size="100"/>
        <column name="regioncode" phpName="RegionCode" type="varchar" size="2"/>
        <column name="country" phpName="Country" type="varchar" size="100"/>
        <column name="countrycode" phpName="CountryCode" type="varchar" size="2"/>

        <column name="latitude" phpName="Latitude" type="float"/>
        <column name="longitude" phpName="Longitude" type="float"/>
        <behavior name="geocodable">
            <parameter name="auto_update" value="false" />
            <parameter name="latitude_column" value="latitude" />
            <parameter name="longitude_column" value="longitude" />
            <parameter name="type" value="DOUBLE" />
            <parameter name="size" value="11" />
            <parameter name="scale" value="8" />
        </behavior>

        <column name="alternate_names" phpName="AlternateNames" type="array"/>

    </table>

    <table name="job_site" phpName="JobSiteRecord" reloadOnUpdate="true" reloadOnInsert="true">
        <column name="jobsite_key" phpName="JobSiteKey" type="varchar" size="100" primaryString="true"
                primaryKey="true"/>
        <column name="plugin_class_name" phpName="PluginClassName" type="varchar" size="100" primaryString="false"
                primaryKey="false"/>
        <column name="display_name" phpName="DisplayName" type="varchar" size="255" primaryString="false"
                primaryKey="false"/>


        <column name="is_disabled" phpName="isDisabled" type="boolean" defaultValue="false"/>

        <column name="date_last_pulled" phpName="LastPulledAt" type="timestamp"/>
        <behavior name="aggregate_column" id="date_last_pulled">
            <parameter name="name" value="date_last_pulled" />
            <parameter name="foreign_table" value="user_search_run" />
            <parameter name="expression" value="MAX(date_ended)" />
            <parameter name="condition" value="run_result_code = 4" />
        </behavior>


        <column name="date_last_run" phpName="LastRunAt" type="timestamp"/>
        <behavior name="aggregate_column" id="date_last_run">
            <parameter name="name" value="date_last_run" />
            <parameter name="foreign_table" value="user_search_run" />
            <parameter name="expression" value="MAX(date_started)" />
        </behavior>

        <column name="date_last_completed" phpName="LastCompletedAt" type="timestamp"/>
        <behavior name="aggregate_column" id="date_last_completed">
            <parameter name="name" value="date_last_completed" />
            <parameter name="foreign_table" value="user_search_run" />
            <parameter name="expression" value="MAX(date_ended)" />
            <parameter name="condition" value="run_result_code = 4" />
        </behavior>

        <column name="date_last_failed" phpName="LastFailedAt" type="timestamp"/>
        <behavior name="aggregate_column" id="date_last_failed">
            <parameter name="name" value="date_last_failed" />
            <parameter name="foreign_table" value="user_search_run" />
            <parameter name="expression" value="MAX(date_ended)" />
            <parameter name="condition" value="run_result_code = 1" />
        </behavior>

        <column name="last_user_search_run_id" phpName="LastUserSearchRunId" type="integer" lazyLoad="true"/>
        <foreign-key foreignTable="user_search_run">
            <reference local="last_user_search_run_id" foreign="user_search_run_id"/>
        </foreign-key>
        <behavior name="aggregate_column">
            <parameter name="name" value="last_user_search_run_id" />
            <parameter name="foreign_table" value="user_search_run" />
            <parameter name="expression" value="MAX(user_search_run_id)" />
        </behavior>
<!--
        <foreign-key foreignTable="user_search_run" onUpdate="none" onDelete="none">
            <reference local="last_user_search_run_id" foreign="user_search_run_id"/>
        </foreign-key>
-->

        <column name="supported_country_codes" phpName="SupportedCountryCodes" type="array"/>
        <column name="results_filter_type" phpName="ResultsFilterType" type="enum"
                valueSet="all-only, all-by-location, user-filtered"/>
        <behavior name="versionable" />
    </table>


    <table name="user" phpName="User" allowPkInsert="true">
        <column name="user_id" phpName="UserId" type="integer" required="true" autoIncrement="true"
                primaryKey="true"/>

        <column name="user_slug" phpName="UserSlug" type="varchar" size="128" required="true" primaryKey="false"
                primaryString="true"/>
        <behavior name="sluggable">
            <parameter name="slug_column" value="user_slug"/>
            <parameter name="slug_pattern" value="{EmailAddress}"/>
            <parameter name="replace_pattern" value="/[^\w\/]+/u"/>
            <parameter name="replacement" value=""/>
            <parameter name="separator" value="-"/>
            <parameter name="permanent" value="false"/>
        </behavior>

        <column name="name" phpName="Name" type="varchar" size="128" required="true"/>
        <column name="email_address" phpName="EmailAddress" type="varchar" size="128" required="false"/>
        <unique name="uniq_user_email/">
            <unique-column name="email_address"/>
        </unique>

        <column name="configuration_file_path" phpName="ConfigFilePath" type="varchar" size="128" required="false"/>
    </table>


    <table name="user_job_match" phpName="UserJobMatch">
        <column name="user_job_match_id" phpName="UserJobMatchId" type="integer" required="true" primaryKey="true"
                autoIncrement="true"/>
        <column name="user_id" phpName="UserId" type="varchar" size="128" required="true"/>
        <column name="jobposting_id" phpName="JobPostingId" type="integer" required="true"/>
        <column name="user_notification_state" phpName="UserNotificationState" type="enum"
                valueSet="not-ready,ready,sent" defaultValue="not-ready"/>
        <column name="is_job_match" phpName="IsJobMatch" type="boolean"/>
        <column name="is_excluded" phpName="IsExcluded" type="boolean"/>
        <column name="is_include_in_notifications" phpName="IsIncludeInNotifications" type="boolean"/>
        <column name="matched_user_keywords" phpName="MatchedUserKeywords" type="array"/>
        <column name="matched_negative_title_keywords" phpName="MatchedNegativeTitleKeywords" type="array"/>
        <column name="matched_negative_company_keywords" phpName="MatchedNegativeCompanyKeywords" type="array"/>
        <column name="out_of_user_area" phpName="OutOfUserArea" type="boolean"/>
        <column name="app_run_id" phpName="AppRunId" type="varchar" size="75"/>
        <foreign-key foreignTable="user">
            <reference local="user_id" foreign="user_id"/>
        </foreign-key>
        <foreign-key foreignTable="jobposting">
            <reference local="jobposting_id" foreign="jobposting_id"/>
        </foreign-key>
    </table>

    <table name="user_search" phpName="UserSearch" reloadOnInsert="true" reloadOnUpdate="true">
        <column name="user_search_id" phpName="UserSearchId" type="integer" required="true" autoIncrement="true"
                primaryKey="true"/>

        <column name="user_id" phpName="UserId" type="integer" required="true"/>
        <foreign-key foreignTable="user" onUpdate="none" onDelete="none">
            <reference local="user_id" foreign="user_id"/>
        </foreign-key>

        <column name="geolocation_id" phpName="GeoLocationId" type="integer"/>
        <foreign-key foreignTable="geolocation" onUpdate="none" onDelete="none">
            <reference local="geolocation_id" foreign="geolocation_id"/>
        </foreign-key>

        <column name="user_search_key" phpName="UserSearchKey" type="varchar" size="128" required="true" primaryString="true"/>
        <behavior name="sluggable">
            <parameter name="slug_column" value="user_search_key"/>
            <parameter name="slug_pattern" value="{UserId}_{SearchKeyFromConfig}_{GeoLocationId}"/>
            <parameter name="replace_pattern" value="/[^\w\/]+/u"/>
            <parameter name="replacement" value=""/>
            <parameter name="separator" value="_"/>
            <parameter name="permanent" value="false"/>
        </behavior>

        <column name="keywords" phpName="Keywords" type="array"/>
        <column name="keyword_tokens" phpName="KeywordTokens" type="array"/>

        <column name="search_key_from_config" phpName="SearchKeyFromConfig" type="varchar" size="50"/>

        <!--<behavior name="versionable" />-->
        <column name="date_created" phpName="CreatedAt" type="timestamp"/>
        <column name="date_updated" phpName="UpdatedAt" type="timestamp"/>
        <behavior name="timestampable">
            <parameter name="create_column" value="date_created"/>
            <parameter name="update_column" value="date_updated"/>
        </behavior>

        <!--<column name="date_last_run" phpName="LastRunAt" type="timestamp"/>-->
        <!--<behavior name="aggregate_column" id="us_date_last_run">-->
            <!--<parameter name="name" value="date_last_run" />-->
            <!--<parameter name="foreign_table" value="user_search_run" />-->
            <!--<parameter name="expression" value="MAX(date_started)" />-->
        <!--</behavior>-->

        <column name="date_last_completed" phpName="LastCompletedAt" type="timestamp"/>
        <behavior name="aggregate_column" id="us_date_last_completed">
            <parameter name="name" value="date_last_completed" />
            <parameter name="foreign_table" value="user_search_run" />
            <parameter name="expression" value="MAX(date_ended)" />
            <parameter name="condition" value="run_result_code = 4" />
        </behavior>

        <!--<column name="date_last_failed" phpName="LastFailedAt" type="timestamp"/>-->
        <!--<behavior name="aggregate_column" id="us_date_last_failed">-->
            <!--<parameter name="name" value="date_last_failed" />-->
            <!--<parameter name="foreign_table" value="user_search_run" />-->
            <!--<parameter name="expression" value="MAX(date_ended)" />-->
            <!--<parameter name="condition" value="run_result_code = failed" />-->
        <!--</behavior>-->

        <behavior name="versionable" />

    </table>


    <table name="user_search_run" phpName="UserSearchRun" reloadOnInsert="true" reloadOnUpdate="true">
        <column name="user_search_run_id" phpName="UserSearchRunId" type="integer" required="true" autoIncrement="true"
                primaryKey="true"/>

        <!--<column name="search_key" phpName="SearchKey" type="varchar" size="128" required="true"/>-->
        <!--<column name="user_slug" phpName="UserSlug" type="varchar" size="128" required="true"/>-->
        <!--<column name="geolocation_id" phpName="GeoLocationId" type="integer"/>-->
        <!--<foreign-key foreignTable="geolocation" onUpdate="none" onDelete="none">-->
            <!--<reference local="geolocation_id" foreign="geolocation_id"/>-->
        <!--</foreign-key>-->

        <column name="user_search_id" phpName="UserSearchId" type="integer" required="true"/>
        <foreign-key foreignTable="user_search" onUpdate="none" onDelete="none">
            <reference local="user_search_id" foreign="user_search_id"/>
        </foreign-key>

        <column name="app_run_id" phpName="AppRunId" type="varchar" size="75"/>

        <column name="user_search_run_key" type="varchar" size="100" required="true"/>
        <behavior name="sluggable">
            <parameter name="slug_column" value="user_search_run_key"/>
            <parameter name="slug_pattern" value="{UserSearchKey}_{JobSiteKey}_{AppRunId}"/>
            <parameter name="replace_pattern" value="/[^\w\/]+/u"/>
            <parameter name="replacement" value=""/>
            <parameter name="separator" value="-"/>
            <parameter name="permanent" value="false"/>
        </behavior>

        <column name="jobsite_key" phpName="JobSiteKey" type="varchar" size="100" required="true"/>
        <foreign-key foreignTable="job_site">
            <reference local="jobsite_key" foreign="jobsite_key"/>
        </foreign-key>

        <column name="search_start_url" phpName="SearchStartUrl" type="varchar" size="1024"/>
        <column name="run_result_code" phpName="RunResultCode" type="enum"
                valueSet="not-run,failed,excluded,skipped,successful" defaultValue="not-run"/>-->
        <column name="run_error_details" phpName="RunErrorDetails" type="array"/>



        <column name="date_started" phpName="StartedAt" type="timestamp"/>
        <column name="date_ended" phpName="EndedAt" type="timestamp"/>

        <behavior name="delegate">
            <parameter name="to" value="user_search" />
        </behavior>
    </table>


    <!--

    <table name="company" phpName="Company">
        <column name="company_id" phpName="companyId" type="integer" required="true" primaryKey="true" autoIncrement="true"/>
        <column name="name" type="varchar" size="128" required="true"/>
        <column name="logo" type="varchar" size="1024" required="false"/>
        <column name="base_url" phpName="baseUrl" type="varchar" size="1024" required="false"/>
    </table>

    <table name="company_job_postings" isCrossRef="true">
        <column name="company_id" phpName="companyId" type="integer" primaryKey="true"/>
        <column name="job_post_id" phpName="jobPostId" type="integer" primaryKey="true"/>
        <foreign-key foreignTable="company">
            <reference local="company_id" foreign="company_id"/>
        </foreign-key>
        <foreign-key foreignTable="jobposting">
            <reference local="job_post_id" foreign="job_post_id"/>
        </foreign-key>
    </table>

-->
    <!--
        When you're done with editing, open a terminal and run
            `$ cd /Users/bryan/code/jobs_scooper_v4/config`
            `$ vendor/bin/propel build`
        to generate the model classes.

        You should now be able to perform basic crud operations with your models. To learn how to use these models
        please look into our documentation: http://propelorm.org/documentation/03-basic-crud.html
    -->
</database>
